import React from 'react';

const InvitePage = () => {
    return (
        <div>
            <h1>Invite</h1>
            <p>Allow users to send invites via email or generate referral links.</p>
        </div>
    );
};

export default InvitePage;
